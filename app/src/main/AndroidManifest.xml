<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sv.edu.catolica.bolanios.jonathan.descubreelsalvador">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".prueba"></activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".CargarLugares" />
        <activity android:name=".Login" />
        <activity android:name=".AgregarPublicacion" />
        <activity android:name=".PrincipalElSalvador" />
        <activity android:name=".Registrarse" />
        <activity android:name=".PantallaInicio"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>